# The Claude Agent SDK peers zod@^4 but this repo uses zod@3 for its schemas.
# The SDK's runtime does not import zod for the query() path we use, so zod 3 is safe.
# Revisit if/when M3 wires Codex outputSchema (the first code that exercises a schema path).
legacy-peer-deps=true
